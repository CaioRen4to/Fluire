import 'package:flutter/material.dart';

class TelaDetalheAula extends StatelessWidget {
  const TelaDetalheAula({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Detalhes da Aula'),
      ),
      body: const Center(
        child: Text('Tela em desenvolvimento'),
      ),
    );
  }
}
